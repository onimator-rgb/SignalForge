# SignalForge Frontend

Vue 3 + TypeScript + TailwindCSS v4 + Vite

```bash
npm install
npm run dev        # http://localhost:5173
npm run build      # Production build
```

Requires backend running on `http://localhost:8000`. See root [README](../README.md).
